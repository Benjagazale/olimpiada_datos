# Análisis Exploratorio de Datos (EDA)

```{r}
#Cargamos las librerías
library(tidyverse)
library(visdat)
library(dlookr)
library(flextable) 
library(inspectdf) 
library(qqplotr) 
library(ggpmisc)
library(PerformanceAnalytics)
library(corrplot)
library(ggpubr)
library(skimr)
library(janitor)
<<<<<<< HEAD
=======
library(stringr)
>>>>>>> 27239ea027e1c884c6c7be42b2a875a692d48e05
```

## 🔎 Carga del dataset
```{r}
#Importamos el csv
encuesta_df <-read_csv("Encuesta_Satisfaccion_ANONIMIZADA_final.csv", show_col_types = FALSE)

```

## 🔎 Inspección inicial de la estructura
### Dimensión

```{r}
dim(encuesta_df)
```
### Estructura y tipos de datos por columna

💡 Use skim porque son demasiadas variables para mostrarlas con un str o un summay. Además, los nombres de cada columna son preguntas. Sería bueno limpiar los nombre largos y pasarlos a un tipo "nombre_variable". 


```{r}
skim(encuesta_df)
```
<<<<<<< HEAD
#### Tratamiento nombres columnas:

💡 ¿Qué hice?
- Cree un diccionario para reemplazar los nombres de las columnas. 
- Dejé todo en minúscula y sin tildes.
- Se separaba el nombre de la empresa con un guión bajo, así que lo reemplace por el nombre de la empresa. 
‼️ *¿Qué falta?* 
- Renombrar las columnas extensas.
 -> Por ejemplo, que comiencen con el área a la que corresponde; comercial, logística, técnia o digital.
- Análizar si conviene convertir columnas a factor. 


```{r}
# Creamos un diccionario con el nombre original y un ID de columna
diccionario_preguntas <- tibble(
  id = paste0("col_", 1:ncol(encuesta_df)),
  pregunta_original = names(encuesta_df)
)

# Diccionario para saberexactamente qué mide cada columna
View(diccionario_preguntas)

```

```{r}

# Convierte caracteres especiales y minúsculas
df_clean <- encuesta_df %>% clean_names() %>% 
  rename_with(~ str_replace_all(., "scaff_co", "scaffco"))

# Cómo quedaron los nombres limpios:
names(df_clean)[1:5]
```

```{r}

diccionario_nombres <- tibble(nombre_original = names(encuesta_df)) %>%
  mutate(
    tiene_corchete = str_detect(nombre_original, "\\[.+\\]"),
    atributo_extraido = str_extract(nombre_original, "(?<=\\[).+(?=\\])")
  ) %>%
  left_join(equivalencias, by = "nombre_original")
```


```{r}
#  Limpiamos los atributos de corchete uno por uno
diccionario_nombres <- diccionario_nombres %>%
  mutate(
    concepto_bracket = if_else(
      tiene_corchete,
      map_chr(atributo_extraido, make_clean_names),
      NA_character_
    ),
    concepto_final = coalesce(concepto, concepto_bracket)
  )
```

```{r}
# Generar el nombre final único por columna
diccionario_nombres <- diccionario_nombres %>%
  mutate(
    nombre_limpio = make.unique(concepto_final, sep = "_v")
  )

# Verificación: debe dar 0 y 0
diccionario_nombres %>% summarise(
  nas = sum(is.na(nombre_limpio)),
  duplicados = sum(duplicated(nombre_limpio))
)
```

```{r}
# Aplicar al dataset
names(encuesta_df) <- diccionario_nombres$nombre_limpio
names(encuesta_df)

```

```{r}
# Guardar el diccionario para sección 4.2
write_csv(diccionario_nombres, "diccionario_variables.csv")

```

```{r}
missing_por_variable <- encuesta_df %>%
  summarise(across(everything(), ~ round(mean(is.na(.)) * 100, 1))) %>%
  pivot_longer(everything(), names_to = "nombre_limpio", values_to = "pct_missing")

diccionario_nombres <- diccionario_nombres %>%
  left_join(missing_por_variable, by = "nombre_limpio")

write_csv(diccionario_nombres, "diccionario_variables.csv")
```




### Datos Faltantes

```{r}
# Reporte detallado por variable (% de nulos y recuento)
diagnose(encuesta_df) %>% 
  select(variables, types, missing_count, missing_percent)
resumen <- as_tibble(skim(encuesta_df))
candidatas <- resumen %>% 
  filter(skim_type == "character", character.n_unique %in% 6:9) %>%
  select(skim_variable, n_missing, character.n_unique)

print(candidatas, n = 50)
```

```{r}
candidatas_nombres <- candidatas$skim_variable

valores_unicos <- encuesta_df %>%
  select(all_of(candidatas_nombres)) %>%
  map(~ unique(.x)) 

# Vemos los valores de las primeras 5, para partir
valores_unicos[1:5]
```

```{r}
todos_los_valores <- encuesta_df %>%
  select(all_of(candidatas_nombres)) %>%
  unlist() %>%
  unique()

todos_los_valores
```

```{r}
es_escala_1_a_7 <- function(x) {
  valores <- unique(na.omit(x))
  all(valores %in% c("1","2","3","4","5","6","7","N/A"))
}

diagnostico_escalas <- encuesta_df %>%
  select(all_of(candidatas_nombres)) %>%
  map_lgl(es_escala_1_a_7)

# Nombres que SÍ son escalas 1-7 verdaderas
columnas_escala <- names(diagnostico_escalas[diagnostico_escalas])
length(columnas_escala)

# Nombres que se colaron pero NO son escala (para revisar aparte)
columnas_no_escala <- names(diagnostico_escalas[!diagnostico_escalas])
columnas_no_escala
```

```{r}
#tratar N/A como valor faltante y no como categoría
encuesta_df <- encuesta_df %>%
  mutate(across(
    all_of(columnas_escala),
    ~ na_if(.x, "N/A") %>% as.numeric()
  ))
```

```{r}
resumen_final <- as_tibble(skim(encuesta_df))
resumen_final %>% count(skim_type)
```

```{r}
#Evaluar los rangos, entre 1 y 7 estan OK
encuesta_df %>%
  select(all_of(columnas_escala)) %>%
  reframe(across(everything(), ~ range(.x, na.rm = TRUE))) %>%
  unlist() %>%
  unique()
```

```{r}
#Evaluar que columnas tienen minimo 2 o 3 
encuesta_df %>%
  select(all_of(columnas_escala)) %>%
  summarise(across(everything(), ~ min(.x, na.rm = TRUE))) %>%
  pivot_longer(everything(), names_to = "variable", values_to = "minimo") %>%
  filter(minimo > 1)
```

```{r}
#Visualizar patrón de valores faltantes
encuesta_df %>%
  select(all_of(columnas_escala)) %>%
  vis_miss(warn_large_data = FALSE)
```

```{r}
encuesta_df %>% select(all_of(columnas_escala)) %>% vis_dat(warn_large_data = FALSE)
```

```{r}
diagnose(encuesta_df) %>% flextable()
```

```{r}
# Evaluar si las filas con el mismo id_respuesta son la misma persona respondiendo dos veces o son personas distinas a las que se les asigno el mismo id por error?
ids_duplicados <- encuesta_df %>%
  count(id_respuesta, medicion) %>%
  filter(n > 1) %>%
  pull(id_respuesta)

encuesta_df %>%
  filter(id_respuesta %in% ids_duplicados[1:3]) %>%
  select(id_respuesta, medicion, estado_respuesta, periodo, recomendacion) %>%
  arrange(id_respuesta)
```

```{r}
#Evaluar si son filas exactamente duplicadas
duplicados_exactos <- encuesta_df %>%
  filter(id_respuesta %in% ids_duplicados) %>%
  group_by(id_respuesta) %>%
  mutate(fila_identica_a_otra = duplicated(across(everything())) | duplicated(across(everything()), fromLast = TRUE)) %>%
  ungroup()

# ¿Cuántas de las filas duplicadas por id son también 100% idénticas en TODAS las columnas?
duplicados_exactos %>% 
  count(fila_identica_a_otra)
```

```{r}
#Decisión de limpieza
encuesta_df <- encuesta_df %>%
  mutate(id_fila = row_number(), .before = id_respuesta)

# Verificación: ahora sí debe ser 100% único
n_distinct(encuesta_df$id_fila) == nrow(encuesta_df)
```

```{r}
#Respuestas parciales
encuesta_df %>%
  filter(estado_respuesta == "PARTIAL") %>%
  summarise(across(everything(), ~mean(!is.na(.x)))) %>%
  pivot_longer(everything(), names_to = "variable", values_to = "pct_completo") %>%
  summarise(pct_completo_promedio = mean(pct_completo))
#Esto da, en promedio, qué porcentaje de todas las columnas alcanzó a responder una fila PARTIAL típica. Con ese número decidimos con criterio: si completan poquísimo (ej. <15%), lo más sano es excluirlas del análisis cuantitativo (aunque se puede mantener el n=427 mencionado, aclarando cuántas quedan útiles). Si completan una porción razonable, se pueden mantener y dejar que cada análisis use listwise/pairwise deletion según corresponda.
```

```{r}
#Considerando como variable objetivo RECOMENDACION, revisar si en filas PARTIAL contestaron esto y abandonaron el resto
encuesta_df %>%
  filter(estado_respuesta == "PARTIAL") %>%
  summarise(
    n_partial = n(),
    con_recomendacion = sum(!is.na(recomendacion)),
    pct_con_recomendacion = mean(!is.na(recomendacion))
  )
```

```{r}
print(diccionario_nombres, n = Inf, width = Inf)
```

```{r}

```

```{r}
```

```{r}
```

```{r}
```

```{r}
```

```{r}
```

```{r}
```

```{r}
```

```{r}
```

```{r}
```

```{r}
```
